// context holds states, variables and functions and provide them to any component irrespective of heirarchy

import { createContext } from "react";

const transContext = createContext(); // creating a new context

export default transContext;